git init   ==> folder to repostry

git status

git add <filename.filetype>

git commit -m "first version"

git add .

git push

git commit -m "second version"

git add .

git push

git commit -m "last version"
